module Quoting
  class QuotesController < Quoting::ApplicationController
    def new
      site = SessionManager.current_site
      customer = CustomerManager.get(site, customer_id)
      quote = QuoteManager.new_quote(customer_id: customer_id)
    end

    def show
    end
  end
end
